# # marks line as a comment
# * - matches 0 or more characters
# ? - matches 1 character
# [abc] - matches a, b, or c
# ** - matches nested directories - a/**/z matches

# Signature files are only for releases and as such are not to be included within the git repo
*.sig
